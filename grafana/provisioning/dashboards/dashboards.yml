apiVersion: 1

providers:
  - name: 'Vehicle Telemetry Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      # This path refers to where the dashboards folder is mounted inside the Grafana container
      path: /etc/grafana/dashboards